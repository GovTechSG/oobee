<style>
  .header-title {
    padding-bottom: 0 !important;
    margin-bottom: 0;
    color: var(--true-white) !important;
  }

  .inspect-preset-header {
    align-items: flex-start;
    background-color: var(--a11y-majorelle-blue);
    padding-top: 1.5rem;
    padding-bottom: 1.25rem;
    gap: 1.5rem;
  }

  .inspect-preset-logo {
    flex: 0 0 auto;
  }

  .header-copy {
    display: flex;
    min-width: 0;
    flex-direction: column;
    gap: 0.35rem;
  }

  .header-subtitle {
    margin: 0;
    color: var(--true-white);
    font-size: 1.125rem;
    line-height: 1.4rem;
  }

  #accessibility-site-report .inspect-preset-header .header-title {
    font-size: 2.5rem;
    line-height: 2.25rem;
  }

  @media (max-width: 767px) {
    .inspect-preset-header {
      padding-left: 1rem;
      padding-right: 1rem;
    }
  }

  @media (max-width: 575px) {
    .inspect-preset-header {
      gap: 0.75rem;
      padding: 1rem;
    }

    .inspect-preset-logo {
      width: 108px;
      height: auto;
    }

    #accessibility-site-report .inspect-preset-header .header-title {
      font-size: 2rem;
      line-height: 1.75rem;
    }

    #accessibility-site-report .inspect-preset-header .header-subtitle {
      font-size: 0.875rem !important;
      line-height: 1.25rem;
    }
  }

  @media (max-width: 375px) {
    .inspect-preset-logo {
      width: 100px;
    }

    #accessibility-site-report .inspect-preset-header .header-title {
      font-size: 1.875rem;
      line-height: 1.625rem;
    }
  }
</style>
