<style>
  #failed-criteria-card {
    display: flex;
    flex-direction: column;
  }

  #failed-criteria-card li:not(:last-of-type) {
    border-bottom: 1px solid var(--chinese-silver);
    padding-bottom: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .wcag-row-column {
    display: flex;
    flex-direction: column;
  }

  .view-all-link {
    align-self: center;
    padding: 0.5rem 0;
    border: none;
    background: transparent;
    color: var(--a11y-majorelle-blue);
    font-size: 1rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: color 0.2s ease;
  }

  .view-all-link:hover {
    color: var(--a11y-majorelle-blue-dark);
    text-decoration: underline;
  }

  .view-all-link svg {
    flex-shrink: 0;
  }

  @media (max-width: 768px) {
    .failed-criteria-list {
      max-height: 300px;
    }

    .failed-criteria-item {
      padding: 0.5rem 0.75rem;
    }
  }
</style>
