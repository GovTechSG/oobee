<style>
  .website-title {
    word-break: break-word;
  }

  #aboutScanModalLabel {
    margin-bottom: 1rem;
  }

  #scan-about > ul {
    padding-left: 0;
    padding-top: 0;
    padding-bottom: 1rem;
    margin-top: 0;
    margin-bottom: 0;
  }

  #scan-about li {
    display: flex;
    line-height: 1.1rem;
    margin-bottom: 2rem;
  }

  #scan-about li > svg {
    flex-shrink: 0;
    margin-left: 0.3rem;
    margin-right: 0.6rem;
  }

  .about-scan-details-left {
    width: var(--left-col);
    padding-right: var(--gap);
  }

  .url-scanned-text {
    color: var(--a11y-majorelle-blue);
    word-break: break-word;
  }

  .wcag-criteria-label {
    text-decoration: none;
    margin-bottom: 0.3rem;
  }

  #scan-about li.advanced-group {
    align-items: flex-start;
    margin-bottom: 0;
  }

  #scan-about li.advanced-group > .advanced-group-content {
    min-width: 0;
    flex: 1;
  }

  #scan-about li.advanced-group > .advanced-group-content > ul.advanced-sublist {
    list-style: disc;
    list-style-position: outside;
    margin-left: 8px;
    margin-top: 0.5rem;
    padding-left: 12px;
    text-indent: -2px;
  }

  #scan-about li.advanced-group > .advanced-group-content > ul.advanced-sublist li {
    margin: 0;
    list-style-position: outside !important;
  }

  .advanced-sublist-li {
    display: list-item !important;
    margin-bottom: 0.3rem !important;
  }

  .advanced-sublist-li:first-child {
    margin-top: 0.5rem !important;
  }

  .advanced-sublist-li:last-child {
    margin-bottom: 2rem !important;
  }

  .about-scan-toggle {
    border: none;
    background-color: var(--true-white);
    text-align: left;
    padding: 0;
    text-decoration: none;
    cursor: pointer;
    display: flex;
    flex-direction: column;
    max-width: 100%;
  }

  .about-scan-toggle:hover {
    text-decoration: none;
  }

  #scan-about li.about-scan-toggle-active {
    background-color: var(--a11y-majorelle-blue);
    border-radius: 4px;
    padding: 0.25rem 0.5rem;
    margin-left: -0.5rem;
  }

  #scan-about li.about-scan-toggle-active > svg path {
    fill: var(--true-white);
  }

  .about-scan-toggle[aria-expanded="true"] .about-us-type-of-scan-text,
  .about-scan-toggle[aria-expanded="true"] .about-scan-link-text,
  .about-scan-toggle[aria-expanded="true"] .wcag-criteria-label {
    color: var(--true-white) !important;
  }

  .about-scan-toggle[aria-expanded="true"] {
    background-color: transparent;
  }

  .about-scan-toggle[aria-expanded="true"] .about-scan-link-chev path {
    fill: var(--true-white);
  }

  .about-scan-toggle .about-us-type-of-scan-text {
    color: var(--a11y-majorelle-blue) !important;
    display: inline;
    text-decoration: underline;
    cursor: pointer;
  }

  #pagesScannedModalToggleTxt {
    color: var(--a11y-majorelle-blue) !important;
  }

  .about-scan-toggle[aria-expanded="true"] #pagesScannedModalToggleTxt {
    color: var(--true-white) !important;
  }

  .about-scan-toggle:hover .about-us-type-of-scan-text,
  .about-scan-toggle .about-us-type-of-scan-text:focus-visible {
    text-decoration: underline;
  }

  .about-scan-link{
    color: var(--a11y-majorelle-blue);
    text-decoration: none;
    cursor: pointer;
  }
  .about-scan-link:hover,
  .about-scan-link:focus-visible,
  .about-scan-toggle:hover .about-scan-link {
    text-decoration: underline;
  }

  .about-scan-link-row {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    line-height: 1.2;
  }

  .about-scan-divider-item {
    display: block !important;
    margin-bottom: 2rem !important;
  }

  .about-scan-link-text {
    color: var(--a11y-majorelle-blue);
    text-decoration: underline;
    cursor: pointer;
  }

  .about-scan-link-text:hover,
  .about-scan-toggle:hover .about-scan-link-text,
  .about-scan-link-text:focus-visible {
    text-decoration: underline;
  }

  .about-scan-link-chev {
    position: relative;
    top: 0.1rem;
  }

  .oobee-version-text {
    font-size: 1.125rem;
    font-weight: 600;
  }

  @media (max-width: 991.98px) {
    #aboutScanModal .modal-dialog{ --bs-modal-width: calc(100vw - 1rem); }
    #aboutScanModal .modal-header::after,

    .about-scan-details-left{ width:auto; padding-right:0; }

    #aboutScanModal .modal-dialog.modal-dialog-centered{
      align-items: flex-start;
      min-height: auto;
    }
  }

</style>
