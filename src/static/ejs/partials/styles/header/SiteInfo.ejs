<style>
  #site-header {
    display: flex;
    flex-wrap: wrap;
  }

  #site-container {
    display: flex;
    gap: 2rem;
    align-items: center;
    width: 100%;
  }

  #site-container > div {
    min-width: 0;
    flex-shrink: 1;
  }

  #site-container > div:nth-child(1),
  #site-container > div:nth-child(2) {
    overflow: hidden;
  }

  #site-container > div:nth-child(1) {
    max-width: 48ch;
  }

  #site-container > div:nth-child(2) {
    max-width: 56ch;
  }

  #site-container > div:nth-child(3) {
    flex-shrink: 0;
    width: auto;
  }

  .inspect-preset-site-header #site-container {
    display: grid;
    grid-template-columns: fit-content(48ch) fit-content(38ch) max-content;
    justify-content: start;
    gap: 0.5rem 1.25rem;
  }

  .inspect-preset-site-header > .d-flex {
    min-width: 0;
    width: 100%;
  }

  .inspect-preset-site-header #site-container > div:nth-child(4) {
    grid-row: 2;
    grid-column: 1 / -1;
  }

  .inspect-preset-viewport-details {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.5rem 2rem;
  }

  .inspect-preset-details-chevron {
    width: 10px;
    height: 12px;
  }

  #site-container > div:nth-child(1) > div,
  #site-container > div:nth-child(2) {
    overflow: hidden;
  }

  #siteName {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    width: 100%;
  }

  #siteUrl {
    display: inline-flex !important;
    align-items: center;
    gap: 0.25rem;
    min-width: 0;
    vertical-align: middle;
  }

  #siteUrl .url-text {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    min-width: 0;
  }

  #siteUrl svg {
    flex-shrink: 0;
  }

  @media (max-width: 1300px) {
    #site-container {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.5rem;
    }

    .inspect-preset-site-header #site-container {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 0.5rem;
    }

    #site-container > div:nth-child(1),
    #site-container > div:nth-child(2) {
      overflow: visible;
    }

    #site-container > div:nth-child(1) > div {
      overflow: visible;
    }

    #siteName {
      white-space: normal;
      word-wrap: break-word;
      word-break: break-word;
      overflow: visible;
    }

    #siteUrl {
      display: inline-flex !important;
    }

    #siteUrl .url-text {
      white-space: normal;
      word-wrap: break-word;
      word-break: break-word;
      overflow: visible;
    }

    .inspect-preset-site-header #site-container > div:nth-child(1),
    .inspect-preset-site-header #site-container > div:nth-child(2),
    .inspect-preset-site-header #site-container > div:nth-child(3),
    .inspect-preset-site-header .inspect-preset-viewport-details {
      width: 100%;
      max-width: none;
    }

    .inspect-preset-site-header #site-container > div:nth-child(1),
    .inspect-preset-site-header #site-container > div:nth-child(1) > div {
      overflow: hidden;
    }

    .inspect-preset-site-header #site-container > div:nth-child(1) > div {
      min-width: 0;
    }

    .inspect-preset-site-header #siteName {
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      word-break: normal;
      word-wrap: normal;
    }

    .inspect-preset-site-header .inspect-preset-viewport-details {
      align-items: flex-start;
      flex-direction: column;
      gap: 0.5rem;
    }

    .inspect-preset-site-header .inspect-preset-details-link {
      margin-left: 2rem;
    }
  }

  #site-container svg {
    flex-shrink: 0;
    min-width: 24px;
    min-height: 24px;
  }

  #site-container .inspect-preset-details-chevron {
    min-width: 10px;
    min-height: 12px;
  }

  .label {
    margin-bottom: 0 !important;
  }

  .link {
    color: var(--a11y-majorelle-blue);
    text-decoration: underline;
  }

  .link:hover {
    text-decoration: underline;
  }
</style>
